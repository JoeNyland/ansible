---
- name: Install
  apt:
    name: samba

- name: Configure
  copy:
    dest: /etc/samba/smb.conf
    src: smb.conf
    mode: '640'
  notify: Reload Samba

- name: Configure service
  service:
    name: smbd
    state: started
    enabled: true
  register: samba_service
